[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "file-tagger"
version = "0.1.0"
description = "Thin wrapper for the file-tagger Node.js CLI."
requires-python = ">=3.8"

[project.scripts]
file-tagger = "file_tagger.__main__:main"
